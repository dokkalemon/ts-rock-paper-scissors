import { styled } from '@mui/material'

export const Container = styled('div')({
    height: '100vh',
    width: '100%',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
})